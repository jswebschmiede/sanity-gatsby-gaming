import React from "react";
import { Link } from "gatsby";

const Header = () => (
  <header>
    <Link to='/'>Home</Link>
    <Link to='/contact'>Contact</Link>
  </header>
);

export default Header;
